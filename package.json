{
  "name": "phpstan-ls-lite",
  "version": "1.0.0",
  "description": "A lightweight and fast language server that wraps PHPStan.",
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "bin": {
    "phpstan-ls-lite": "./dist/server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "prepack": "pnpm run build",
    "test": "pnpm run build && node --test \"dist/**/*.test.js\""
  },
  "keywords": [],
  "author": "",
  "license": "MPL-2.0",
  "packageManager": "pnpm@10.30.0",
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
